const inquirer = require('inquirer');

async function promptUser() {
  // Implement the Inquirer prompt for user input as per the acceptance criteria
  // Return an object with user input (text, textColor, shape, and shapeColor)
}

module.exports = { promptUser };
